---
- name: Gather info from switches
  hosts: switches
  gather_facts: no

  tasks:
    - name: Show version
      cisco.ios.ios_command:
        commands:
          - show version
      register: output

    - name: Display output
      debug:
        var: output.stdout_lines
